export type RecruitmentType = {
  recruitmentId: number;
  semester: string;
  round: string;
  startDate: string;
  endDate: string;
  fee: number;
};

export type SemesterVariantType = "FIRST" | "SECOND";
